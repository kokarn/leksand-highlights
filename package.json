{
    "name": "shl-highlights-notify",
    "version": "2.10.0",
    "description": "GamePulse API server with push notifications for goals and highlights",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "node --watch --env-file=.env server.js",
        "build": "echo 'Build complete'",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@resvg/resvg-js": "^2.6.2",
        "cors": "^2.8.5",
        "express": "^5.2.1"
    }
}
