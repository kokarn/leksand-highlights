{
    "name": "shl-highlights-notify",
    "version": "2.2.1",
    "description": "SHL game API server with push notifications for highlights",
    "main": "server.js",
    "scripts": {
        "start": "node server.js",
        "dev": "node server.js",
        "build": "echo 'Build complete'",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "@resvg/resvg-js": "^2.6.2",
        "cors": "^2.8.5",
        "express": "^5.2.1"
    }
}
