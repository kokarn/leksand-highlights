{
    "name": "shl-highlights-notify",
    "version": "1.1.0",
    "description": "Notifies when SHL game highlights are available for all teams",
    "main": "shl_notifier.js",
    "scripts": {
        "start": "node shl_notifier.js",
        "build": "echo 'Build complete'",
        "test": "echo \"Error: no test specified\" && exit 1"
    }
}