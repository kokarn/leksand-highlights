name: Android App Build

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'shl-highlights-app/**'
      - '.github/workflows/eas-build.yml'

jobs:
#   eas-build:
#     name: Build on EAS
#     runs-on: ubuntu-latest
#     outputs:
#       app_version: ${{ steps.app_version.outputs.version }}
#       eas_success: ${{ steps.eas_build.outcome == 'success' }}
#     steps:
#       - uses: actions/checkout@v5

#       - uses: actions/setup-node@v6
#         with:
#           node-version: 22
#           cache: npm
#           cache-dependency-path: shl-highlights-app/package-lock.json

#       - name: Setup Expo and EAS
#         uses: expo/expo-github-action@v8
#         with:
#           eas-version: latest
#           token: ${{ secrets.EXPO_TOKEN }}

#       - name: Install dependencies
#         run: npm ci
#         working-directory: ./shl-highlights-app

#       - name: Create google-services.json
#         run: echo "${{ secrets.GOOGLE_SERVICES_JSON }}" | base64 --decode > google-services.json
#         working-directory: ./shl-highlights-app

#       - name: Read app version
#         id: app_version
#         run: echo "version=$(node -p 'require("./shl-highlights-app/app.json").expo.version')" >> "$GITHUB_OUTPUT"

#       - name: Build on EAS (preview)
#         id: eas_build
#         continue-on-error: true
#         run: eas build --platform android --profile preview --non-interactive
#         working-directory: shl-highlights-app

  local-build:
    name: Build locally (fallback)
    runs-on: ubuntu-latest
    permissions:
      contents: write
    # needs: eas-build
    # if: needs.eas-build.outputs.eas_success != 'true'
    steps:
      - name: Setup repo
        uses: actions/checkout@v5

      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: 'npm'
          cache-dependency-path: shl-highlights-app/package-lock.json

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci
        working-directory: shl-highlights-app

      - name: Create google-services.json
        run: echo "${{ secrets.GOOGLE_SERVICES_JSON }}" | base64 --decode > google-services.json
        working-directory: shl-highlights-app

      - name: Verify google-services.json
        run: |
          echo "=== Checking google-services.json ==="
          if [ -f google-services.json ]; then
            echo "File exists!"
            echo "Size: $(stat -c%s google-services.json 2>/dev/null || stat -f%z google-services.json) bytes"
            echo "First 50 chars: $(head -c 50 google-services.json)"
            echo "..."
          else
            echo "ERROR: google-services.json was NOT created!"
            exit 1
          fi
          echo ""
          echo "=== Directory contents ==="
          ls -la
        working-directory: shl-highlights-app

      - name: Read app version
        id: app_version
        run: echo "version=$(node -p 'require("./shl-highlights-app/app.json").expo.version')" >> "$GITHUB_OUTPUT"

      - name: Build Android app
        env:
          GOOGLE_SERVICES_JSON_BASE64: ${{ secrets.GOOGLE_SERVICES_JSON }}
        run: eas build --platform android --profile preview --local --output ${{ github.workspace }}/GamePulse-${{ steps.app_version.outputs.version }}.apk
        working-directory: shl-highlights-app

      - name: Upload APK artifact
        id: upload_apk
        uses: actions/upload-artifact@v4
        with:
          name: GamePulse-${{ steps.app_version.outputs.version }}
          path: ${{ github.workspace }}/GamePulse-${{ steps.app_version.outputs.version }}.apk

      - name: Publish GitHub Release with APK
        id: release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.app_version.outputs.version }}
          name: GamePulse v${{ steps.app_version.outputs.version }}
          body: |
            Automated GamePulse Android build v${{ steps.app_version.outputs.version }}.

            Install directly from the app: **Settings → App Updates → Download & install**.
            Or download the APK below and open it to install.
          files: ${{ github.workspace }}/GamePulse-${{ steps.app_version.outputs.version }}.apk
          make_latest: true
          fail_on_unmatched_files: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Notify ntfy with release link
        if: success()
        env:
          NTFY_TOPIC: "gamepulse-release"
          APP_VERSION: ${{ steps.app_version.outputs.version }}
          RELEASE_URL: ${{ steps.release.outputs.url }}
        run: |
          curl -s -X POST "https://ntfy.sh/${NTFY_TOPIC}" \
            -H "Title: GamePulse v${APP_VERSION}" \
            -H "Priority: default" \
            -H "Tags: package,rocket" \
            -H "Actions: view, View Release, ${RELEASE_URL}" \
            -d "New Android build published. Update in-app via Settings → App Updates."
